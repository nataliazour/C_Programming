#include <stdio.h>

int main()
{
    int A, B;

    printf("Enter FIRST number: ");
    scanf("%d", &A);

    printf("Enter SECOND number: ");
    scanf("%d", &B);

    printf("Result: %d", A + B);

    return 0;
}